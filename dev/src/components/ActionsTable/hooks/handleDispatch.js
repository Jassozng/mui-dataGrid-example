import tableActionReducer from '../../../reducers/TableReducers/tableReducers.js'

const dispatch = (state, actionType) => {
    const action = {
        "type": actionType,
        "payload": ""
    }
}

export default dispatch;